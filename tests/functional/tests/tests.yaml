charm_name: local-users
gate_bundles:
  - noble
  - jammy
  - focal
dev_bundles:
  - noble
tests:
  - tests.test_local_users.TestLocalUsers
target_deploy_status:
  local-users:
    workload-status: active
    workload-status-message-regex: "^$"
  ubuntu:
    workload-status: active
    workload-status-message-regex: "^$"
